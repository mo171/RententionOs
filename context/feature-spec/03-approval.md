# RetentionOS Approval Page 

### read the `AGENTS.md` first

## Objective

Build the `/Approval` page for RetentionOS.

The final UI must visually match the reference image provided ain the chat as closely as possible.

This is an enterprise AI operating dashboard for retention intelligence, autonomous customer interventions, and live operational monitoring.

Do not redesign or reinterpret the UI.
Replicate layout hierarchy, spacing, proportions, component structure, and interaction behavior from the reference image.

## The architect
- the sidebar and the top bar is made you can reuse the component from the overview page
- I want the main dashboard to be scrollable.
- there should be a fixed hieght the layouts of compoenent so that when new data comes it should just add in list not distrupt the layout and expand below damaging the sturcute add scroll if needed 




- the data of approval will keep coming but the data that has already arived will not be change so think accordingly how to handle this 

-for now we will be using mock data so the real  data can be added later mark has pending (the data will but tell me the implementation plan for our u.i to accept it so that without refresh the new approval list can also be seen and the old ones are still there  )

- the animations should be good and should feel premium