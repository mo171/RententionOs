Read `AGENTS.md` before starting.

edit the globals.css file according to the attached file `ui-context.md`. 

We're adding the design system and UI primitive components.
Install and configure `shadcn/ui`.

Add these `shadcn` components:
- add all shadcn ui components
- use shadcn compoenent base for creating components

Do not modify the generated `components/ui/*` files after installation.

Also install `lucide-react`.

Create `lib/utils.ts` with a reusable `cn()` helper for merging Tailwind classes.

ensure all components match the design theme created in `globals.css`.


Check when done
- All components import without errors
- `cn()` works properly
- No default styling our own styling should be there


